import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "#eff1f3",
        },
        blue: { 
            main: "#eff1f3",
            light: "#7cc1e3", 
            dark: "#1892ce", 
        },
    },
});

export default theme;
