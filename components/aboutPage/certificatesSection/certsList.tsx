import { Course } from "@/types/course";

interface CertsListProps {
    courses: Course[];
}

const CertsList = ({ courses }: CertsListProps) => {
    return <div>Enter</div>;
};

export default CertsList;
